#include<stdio.h>

/*
 * Binary Search: -
 * =============
 * 	Time complexity: -
 * 		Best Case    -> O(1)
 * 		Average Case -> O(log n)
 * 		Worst Case   -> O(log n)
 * */

int binary_search(int arr[], int value, int size)
{

	return value;
}

int main(void)
{
	int size;
	scanf("%d",&size);
	int arr[size], i;
	for(i = 0; i < size; i++)
	{

	}
	return 0;
}
