#include<sys/socket.h>  //header file for socket programming
int main() {

/*
    Socket Creation             : socket(int domain, int type, int protocol);
    Socket Binding              : bind(int sockfd, struct sockaddr * myaddr, scklen_t addrlen);
    Socket Listening            : listen(int sockfd, int backlog);
    Socket Acccepting           : accept(int sockfd, struct sockaddr * addr, socklen_t * addrlen);
    Socket Connection           : connect(int sockfd struct sockaddr * serv_addr, socklen_t addrlne);
*/

	socket()

    return 0;
}